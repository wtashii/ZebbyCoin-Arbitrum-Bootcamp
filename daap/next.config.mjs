/** 
 * Next.js configuration file that sets up the image domains for the application.
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: ['cdn.discordapp.com'],
  },
};

export default nextConfig;